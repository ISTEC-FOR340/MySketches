/**
 * script.js
 *
 * Simple logic for the landing page.
 * Note: Navigation is now handled by semantic <a> tags in index.html
 * for better accessibility and native keyboard support.
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log('Landing page ready.');
});
